


NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Optional redirects used by the Clerk UI (these are the app's routes)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# -----------------------------------------------------------------
# Database (Neon/Postgres)
# - Provide a full DATABASE_URL connection string.
# - Example format:
#   postgresql://<user>:<password>@<host>/<database>?sslmode=require
# -----------------------------------------------------------------
#DATABASE_URL=postgresql://username:password@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://neondb_owner:your_db_password@your-neon-host/CareerShotDB?sslmode=require&channel_binding=require

# -----------------------------------------------------------------
# Google Cloud / Dialogflow (Service Account Credentials)
# Two supported approaches:
# 1) Point to a JSON file on disk using GOOGLE_APPLICATION_CREDENTIALS (path)
# 2) Paste the JSON as a single-line string into GOOGLE_APPLICATION_CREDENTIALS_JSON
#    (this project uses the single-line JSON env var). If you paste the JSON,
#    replace real newlines in the private_key with literal "\\n" and wrap the
#    entire JSON in single quotes.
# -----------------------------------------------------------------
# Example (single-line JSON, DO NOT COMMIT):
# GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"...","client_id":"..."}'
GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"your-project-id","private_key_id":"your_key_id","private_key":"-----BEGIN PRIVATE KEY-----\\nYOUR_PRIVATE_KEY_CONTENT_HERE\\n-----END PRIVATE KEY-----\\n","client_email":"your-sa@your-project.iam.gserviceaccount.com","client_id":"your_client_id"}'

# Your GCP project and Dialogflow CX agent ID
GCP_PROJECT_ID=your-gcp-project-id
DIALOGFLOW_AGENT_ID="your-dialogflow-cx-agent-id"


# -----------------------------------------------------------------
# Runtime / environment
# Set NODE_ENV to 'development' locally and 'production' in deployment.
# -----------------------------------------------------------------
NODE_ENV=development
